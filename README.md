# Web component using Angular Ivy

Sample of how to create a web component based on Angular Ivy.

## Run this sample

* `npm start` - run into browser `http://localhost:8080`
* `npm run build` - build into `dist` folder

